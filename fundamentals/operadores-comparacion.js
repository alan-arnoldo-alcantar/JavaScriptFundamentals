// == comparador en base al valor
// === comparador en base al valor y al tipo de dato
// != desigualdad en base al valor
// !== desigualdad en base al valor y al tipo de dato
// > mayor en base al valor
// >= mayor o igual en base al valor
// < menor en base al valor
// <= menor o igual en base al valor

const a = 10;
const b = 20;
const c = "10";

console.log(a == b);
console.log(a === c);
console.log(a!=b);
console.log(a !== c);